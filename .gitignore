# The real key generator holds the salt. It lives on one machine and never here.
keygen.html

# Private business document. Netlify serves the repository root, so anything
# committed here is publicly fetchable. Pricing, kill criteria and honest
# weaknesses stay on the local machine.
LAUNCH.md
docs/

# Local staging and QA artefacts
_deploy/
_ogsrc.html
_qa-*
_ux-*
_f-*
_v-*
_live-*
*.pdf

# Screenshots are noise, but the social preview card is a real shipped asset.
*.png
!og-image.png

# OS and editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
